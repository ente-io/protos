syntax = "proto3";

package ente.common;
// CenterBox is a box where x,y is the center of the box
message CenterBox {
    optional float x = 1;
    optional float y = 2;
    optional float height = 3;
    optional float width = 4;
}